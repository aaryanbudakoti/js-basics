"use strict" ; //treat all js code as newer version , (good syntaxical practice)

// alert(3+3) // we are using node.js and not browser.
// alert is a pop-up print feature which is majorly used by web server binded JS, not node.js (which is being currently used here)

console.log(3+3)
console.log("aaryan")

// code readability is imp 

// JS documentation is present in ECMAscript, 

let name = "aaryan" // string
let age= 21 //number
let isLoggedIn= true // bool
let state; // value is undefined, assigned unintentionally
// null is assigned intentionally 
let users=null
// number, string = "aaryan budakoti ", boolean = true/ false , bigInt , null (stand-alone value), undefined
// symbol = unique


//object 

console.log(typeof undefined); // undefined
console.log(typeof null); // object - null is an object 



console.log(users)